apiVersion: v1
kind: Service
metadata:
  name: gpu-scheduler
  namespace: kube-system
spec:
  selector:
    app: gpu-scheduler
  ports:
    - name: http
      port: 8888
      targetPort: 8888
    - name: metrics
      port: 9090
      targetPort: 9090