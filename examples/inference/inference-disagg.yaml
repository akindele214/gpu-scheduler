apiVersion: v1
kind: Service
metadata:
  name: inference-prefill-svc
  namespace: default
  labels:
    app: inference-disagg
    role: prefill
spec:
  type: NodePort
  selector:
    app: inference-disagg
    role: prefill
  ports:
    - name: http
      port: 8000
      targetPort: 20001
      nodePort: 30081
---
apiVersion: v1
kind: Service
metadata:
  name: inference-decode-svc
  namespace: default
  labels:
    app: inference-disagg
    role: decode
spec:
  type: NodePort
  selector:
    app: inference-disagg
    role: decode
  ports:
    - name: http
      port: 8000
      targetPort: 20002
      nodePort: 30082
---
apiVersion: v1
kind: Pod
metadata:
  name: inference-prefill
  namespace: default
  labels:
    app: inference-disagg
    role: prefill
  annotations:
    gpu-scheduler/workflow: "inference"
    gpu-scheduler/priority: "90"
    gpu-scheduler/preemptible: "false"
    gpu-scheduler/memory-mb: "20000"
    gpu-scheduler/gpu-count: "1"
    gpu-scheduler/shared: "false"
    gpu-scheduler/inference-role: "prefill"
    gpu-scheduler/model-group: "Qwen/Qwen2.5-7B-Instruct"
    gpu-scheduler/inference-endpoint: "http://127.0.0.1:30081"
spec:
  schedulerName: gpu-scheduler
  restartPolicy: Always
  containers:
    - name: vllm-prefill
      image: vllm/vllm-openai:latest
      imagePullPolicy: IfNotPresent
      env:
        - name: VLLM_USE_DEEP_GEMM
          value: "0"
        - name: VLLM_MOE_USE_DEEP_GEMM
          value: "0"
        - name: VLLM_BLOCKSCALE_FP8_GEMM_FLASHINFER
          value: "0"
        - name: VLLM_NIXL_SIDE_CHANNEL_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: VLLM_NIXL_SIDE_CHANNEL_PORT
          value: "5600"
      command: ["python3", "-m", "vllm.entrypoints.openai.api_server"]
      args:
        - "--host"
        - "0.0.0.0"
        - "--port"
        - "20001"
        - "--model"
        - "Qwen/Qwen2.5-7B-Instruct"
        - "--dtype"
        - "bfloat16"
        - "--kv-cache-dtype"
        - "bfloat16"
        - "--max-model-len"
        - "4096"
        - "--gpu-memory-utilization"
        - "0.8"
        - "--kv-transfer-config"
        - '{"kv_connector":"NixlConnector","kv_role":"kv_producer"}'
      ports:
        - containerPort: 20001
          name: http
        - containerPort: 5600
          name: nixl
      resources:
        limits:
          nvidia.com/gpu: "1"
        requests:
          nvidia.com/gpu: "1"
---
apiVersion: v1
kind: Pod
metadata:
  name: inference-decode
  namespace: default
  labels:
    app: inference-disagg
    role: decode
  annotations:
    gpu-scheduler/workflow: "inference"
    gpu-scheduler/priority: "90"
    gpu-scheduler/preemptible: "false"
    gpu-scheduler/memory-mb: "20000"
    gpu-scheduler/gpu-count: "1"
    gpu-scheduler/shared: "false"
    gpu-scheduler/inference-role: "decode"
    gpu-scheduler/model-group: "Qwen/Qwen2.5-7B-Instruct"
    gpu-scheduler/inference-endpoint: "http://127.0.0.1:30082"
spec:
  schedulerName: gpu-scheduler
  restartPolicy: Always
  containers:
    - name: vllm-decode
      image: vllm/vllm-openai:latest
      imagePullPolicy: IfNotPresent
      env:
        - name: VLLM_USE_DEEP_GEMM
          value: "0"
        - name: VLLM_MOE_USE_DEEP_GEMM
          value: "0"
        - name: VLLM_BLOCKSCALE_FP8_GEMM_FLASHINFER
          value: "0"
        - name: VLLM_NIXL_SIDE_CHANNEL_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: VLLM_NIXL_SIDE_CHANNEL_PORT
          value: "5601"
      command: ["python3", "-m", "vllm.entrypoints.openai.api_server"]
      args:
        - "--host"
        - "0.0.0.0"
        - "--port"
        - "20002"
        - "--model"
        - "Qwen/Qwen2.5-7B-Instruct"
        - "--dtype"
        - "bfloat16"
        - "--kv-cache-dtype"
        - "bfloat16"
        - "--max-model-len"
        - "4096"
        - "--gpu-memory-utilization"
        - "0.8"
        - "--kv-transfer-config"
        - '{"kv_connector":"NixlConnector","kv_role":"kv_consumer"}'
      ports:
        - containerPort: 20002
          name: http
        - containerPort: 5601
          name: nixl
      resources:
        limits:
          nvidia.com/gpu: "1"
        requests:
          nvidia.com/gpu: "1"
